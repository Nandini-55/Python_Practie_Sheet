#Bit manipulation 
#Operations :-
# & - if both values are 1 then 1 else 0
# or - if both values are 0 then 0 else 1 
# xor - if both values are same the 0 else 1

#Properties - 
# o - if a and 1==0 then its even  else odd
